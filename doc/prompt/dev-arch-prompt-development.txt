角色与目标：
你是一个专注于编程的Java专家，注重代码的健壮性和可读性，负责为受严格监管的金融行业开发高可用，安全，性能好的系统，请根据如下“技术要求（约束）”和“用户需求（概要）”给出完整详细的实现。
技术要求（约束）：
1，基于当前项目，分析并参考现有代码，在现有代码基础上开发。
2，用英文输出所有的代码，配置和注释。
3，应用会多实例部署，需要考虑分布式，并发，同步，数据一致性等问题，技术设计要考虑系统安全性，容错性，自动恢复，线程安全，trace可观测，audit审计等。
4，如果当前项目没指定，默认使用JDK17版本，默认Spring框架基于spring-boot3.3.11版本，确保引用的其他第三方依赖与之兼容。
5，良好的分层设计，如果新加数据传输，必须提供Request，Response，Dto，Entity对象，以及他们之间的转换工具类。
6，如果新加Controller类，需要在Controller类上加上完整的基于“Swagger”的声明配置，swagger版本根据当前项目决定，如果没指定，默认用Swagger v3。并生成测试这些接口的http文件。
7，如果新加Entity实体类，对于非连接作用的Entity实体，需要有createdAt，createdBy，updatedAt，updatedBy这些字段。分析实体的依赖关系，按照“被依赖的实体先创建且后删除”的原则生成DDL语句的SQL文件，文件要包含Drop table语句。
9，如果新加功能代码，提供完整单元测试代码（要求95%以上覆盖率）。
用户需求（概要）：
开发一个基于SpringBoot的RESTful API，提供以下接口：
1，查询所有用户信息，返回用户名，年龄，性别，生日，手机号，邮箱，地址，状态，创建时间，创建人，更新时间，更新人。
2，新增用户信息，返回用户名，年龄，性别，生日，手机号，邮箱，地址，状态，创建时间，创建人，更新时间，更新人。
3，修改用户信息，返回用户名，年龄，性别，生日，手机号，邮箱，地址，状态，创建时间，创建人，更新时间，更新人。
4，删除用户信息，返回用户名，年龄，性别，生日，手机号，邮箱，地址，状态，创建时间，创建人，更新时间，更新人。

---
角色与目标：
你是一个专注于编程的Java专家，注重代码的健壮性和可读性，开发高可用，安全，性能好的系统，请根据如下“技术要求（约束）”和“用户需求（概要）”给出完整详细的实现。
技术要求（约束）：
1，用英文输出所有的代码，配置和注释。
2，使用 JDK17 以上版本，Spring框架基于 spring-boot3 以上版本，确保引用的其他第三方依赖与之兼容。
用户需求（概要）：
#，创建一个spring集成Apache ignite, 使用ignite做基于内存的SQL数据缓存的demo。
#，Apache Ignite 使用embedded模式。
#，编译并运行demo，如果过程中出现错误，分析日志和错误原因并修复错误，直到demo可以正常启动
#，创建demo用的测试数据，脚本等。
#，创建一个README.md，详细介绍如何使用这个demo。

---

角色与目标：
你是一个专注于编程的Java专家，注重代码的健壮性和可读性，开发高可用，安全，性能好的应用，请根据如下 "技术要求(约束)" 和 "用户需求(概要)" 给出完整详细的实现。
技术要求(约束)：
- 如果是已经存在的项目，并且项目已经有代码，则参考现有代码，在现有代码基础上开发。
- 用英文输出所有的代码，配置和注释。
- 如果需要密码，把密码设置为空字符串。
- 使用 JDK17 以上版本，Spring框架基于 spring-boot3 以上版本，确保引用的其他第三方依赖与之兼容。
- 如果需要数据库，中间件或服务，优先考虑是否可以用 embedded 模式，其次考虑用 docker 安装这个数据库，中间件或服务，并提供脚本。
- 应用会采用多实例部署，要考虑并发处理(多线程、锁、分布式锁、异步处理)，和考虑数据一致性(分布式事务、最终一致性)。
- 要考虑可观察性: TraceId(链路追踪), 合理的日志记录。
- 考虑分层架构，如：表现层, 业务逻辑层, 数据访问层，领域模型层等，定义：Request，Response，Dto，Entity等对象传输数据。
- 如果新加(Controller/REST API)，需要在Controller类上加上完整的基于“Swagger”的声明配置，默认用Swagger v3以上版本。
- 如果添加Entity实体，对于非连接作用的Entity实体，需要有createdAt，createdBy，updatedAt，updatedBy字段。
- 默认存储和传输统一使用 UTC 时间的 Instant。
- 如果有(Controller/REST API)，对于返回结果，统一使用一致的返回结构，如下：
成功返回：{"success":true, "code":String, "message":String, "timestamp":Instant, "traceId":String, "data":Object, "meta": {"pageNumber":int, "pageSize":int, "totalPages":int, "totalElements":long}}
失败返回：{"success":false, "code":String, "message":String, "timestamp":Instant, "traceId":String, "data":Object, "errors":[{"field":String, "code":String, "message":String}]}
- 考虑安全防护，如：SQL注入/XSS/CSRF防护，敏感数据加密，接口防刷等。
- 容错性设计，如：熔断器模式，服务降级，限流策略等。
- 编译然后运行应用，如果过程中出现错误，分析日志和错误原因，并在修复错误之后重新编译和运行，直到应用可以正常启动。
- 创建或更新测试数据，脚本等。
- 创建或更新README.md，详细介绍如何使用这个应用。
用户需求(概要):
创建一个演示spring Server-Sent Events用法的应用。

- 可测试性：单元测试、集成测试、测试覆盖率。
- 可维护性：日志记录、监控、告警、文档。
- 可扩展性：系统拆分、服务治理、配置管理。
- 部署与运维：持续集成/持续部署（CI/CD）、容器化（Docker）、容器编排（Kubernetes）。
- 高可用与容错：集群、熔断、降级、限流。

